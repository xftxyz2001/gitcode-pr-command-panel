# 更新记录

## v1.6.1

- 流水线通知优先读取页面顶部 `.can-edit.title[title]` 的 `title` 属性，并以页面 `<title>` 作为兜底；不再用 `main h1` 或任意 `h1`，避免把 PR 正文的第一个一级标题误作 PR 标题。
- 不再在系统通知关闭事件中抢先删除跳转上下文，避免带详情按钮的失败通知因 Windows 事件顺序而无法从通知正文返回 PR；上下文仍会在点击成功、创建失败或滚动保留时清理。

## v1.6.0

- 点击流水线系统通知正文时，复用并聚焦对应的 GitCode PR 标签页；未打开时才新建标签页，同时兼容 `/pull/` 与 `/merge_requests/` 路由。
- 从 `ascend-robot` 终态评论中提取真实 openLiBing `pipelineDetail` 地址；主流水线失败标签最多等待60秒与新到达的失败评论配对，超时则通知失败但不展示详情按钮。
- 主流水线成功标签立即通知；标签与评论的关联不再扫描历史评论或按时间接近程度猜测，迟到评论不会造成重复通知。
- 有合法详情地址时在通知中显示“查看流水线详情”按钮；地址缺失、域名不符或路径不符时不显示。
- 按通知 ID 在会话存储中隔离 PR 与流水线详情上下文，并在点击、创建失败和滚动保留时清理，避免多个 PR 或多次运行互相串链。
- 本地 Mock 新增机器人评论与流水线详情地址，自动化测试覆盖正文点击、详情按钮、标签页复用、非法链接和上下文清理。

## v1.5.0

- 设置页新增外观配置和实时预览，可自定义命令按钮颜色与悬浮面板背景色，并自动生成具有可读对比度的文字、悬停、按下和边框颜色。
- 支持选择不超过 2 MB 的本地图片作为面板背景，提供覆盖、包含、原始大小居中及背景图片淡化程度设置。
- 外观预览改为读取当前命令配置，新增、删除、改名、启停和排序命令时同步刷新，只展示已启用按钮。
- 外观随命令配置一起持久化；保存后通过扩展存储变更同步更新所有已打开的 GitCode PR 面板，重新打开页面后自动恢复。
- 对图片类型、大小和 Data URL 进行校验，损坏或非法配置自动回退纯色背景；恢复默认会同时清除个性化外观。

## v1.4.0

- 复用 GitCode PR 讨论接口的页面轮询结果，监控新增 `ci-pipeline-passed` 与 `ci-pipeline-failed` 企业标签的事件，并忽略删除标签和普通评论。
- 对应页面不在当前前台（浏览器最小化、切换标签页或切到其他应用）时，通过系统通知显示仓库、PR 编号、PR 标题及流水线状态。
- 首次观察讨论列表时只建立基线，不为历史流水线消息补发通知。
- 设置页提供本地 Mock 测试入口，可在不访问 GitCode 的情况下验证完整系统通知链路。
- 流水线通过通知使用绿色勾选图标，失败通知使用红色叉号图标；Mock 页同时显示通知 API 的成功、抑制或错误结果。
- 扩展自身图标重绘为 `xftxyz2001` 的 GitHub 头像人物手持 GitCode 红色标志，使用透明背景；流水线状态图标保持独立。
- PR 页面识别同时支持 `/pull/<编号>` 与 `/merge_requests/<编号>` 两种 GitCode 前端路由。
- 后台监控改为15秒心跳 watchdog：页面自身仍有 discussions 响应时不重复请求，只有后台页面连续15秒无响应才主动补一次；回到前台后停止补请求，并以30秒 `chrome.alarms` 调度兜底。
- discussions 增量识别改为保存每个 PR 上次响应的最新消息 ID；后续只处理 `sort=desc` 列表中位于该游标之前的新消息，并按时间正序派发，避免扩大 `per_page` 时误处理历史记录。
- 企业标签解析支持一次添加多个逗号分隔标签，并新增 `docs-ci-pipeline-success` / `docs-ci-pipeline-failed` 文档流水线通知。
- 新增 `ascend-robot` 终态评论兜底：跟踪 note 的原地更新，标签优先等待90秒；未出现终态标签时才按“运行失败”或带成功总行的“已完成”评论通知，切回前台会取消等待中的弹窗。

## v1.3.4

- 重复点击齿轮时复用并聚焦已有配置页，不再打开多个配置页标签。
- 调整浏览器窗口尺寸后自动校正悬浮面板位置，避免面板超出右侧或底部可视区域。
- 在极窄或极矮窗口中限制面板尺寸，并允许面板内部滚动。

## v1.3.3

- 删除命令与恢复默认不再使用浏览器原生弹窗，改为与配置页视觉一致的页面内对话框。
- 支持取消、确认、按 Esc 关闭以及点击遮罩取消。

## v1.3.2

- 删除命令前显示确认框，并明确说明保存后才会生效。
- “未保存”状态改为当前表单与最近一次已保存配置的完整比较，避免进入页面时误报。
- 存在真实未保存修改时，关闭、刷新或离开配置页会触发浏览器确认。

## v1.3.1

- 不再从 GitCode 页面内容脚本直接调用部分浏览器未提供的 `chrome.runtime.openOptionsPage()`。
- 点击齿轮后由扩展后台创建配置页标签，并在失败时于悬浮面板显示错误信息。

## v1.3.0

- 新增独立配置页，可配置按钮名称、发送内容、显示状态与顺序。
- 默认命令库覆盖机器人指南中适用于 PR 的命令，并支持添加其他自定义命令。
- 默认仅显示 `compile`、`get-log`、`/lgtm`、`/approve`。
- 页面桥接仅接受当前配置中存在的发送内容。

## v1.2.0

- 根据页面打包代码确认，“发送评论”最终调用 `POST /issuepr/api/v1/projects/{project_id}/merge_requests/{iid}/notes`。
- 请求体与 GitCode 页面一致：`{ body: command, need_to_resolve: false }`。
- 新增主页面桥接脚本，复用 GitCode 当前登录态；不再查找输入框、模拟鼠标、切换选项卡或点击发送按钮。
- 主页面桥接只接受本插件内置的八条命令，避免页面事件被用于发送任意文本。

## v1.1.3

- 已按 GitCode 的实际 DOM 结构锁定底部 `input[placeholder="回复"]`。
- 展开评论框时补齐 `pointerdown`、`mousedown`、聚焦、`pointerup`、`mouseup`、`click` 事件序列，兼容依赖鼠标事件而非单纯 `element.click()` 的页面逻辑。
- 继续监听编辑器的异步挂载，展开后直接找到 CodeMirror 输入区并发送，无需用户手动点击“回复”。

## v1.1.2

- 触发“回复”后使用 `MutationObserver` 监听编辑器挂载和显隐变化。
- 等待窗口延长至 15 秒，兼容 GitCode 评论组件延迟展开。
- 移除连续模拟点击和回车的逻辑，避免页面稍后展开但插件已提前报错。

## v1.1.1

- 不再仅依赖选项卡的 `aria-expanded` 状态。
- 以可见的底部“回复”入口或评论编辑器作为讨论区已就绪的判断依据。
- 修复页面已经位于“讨论”时仍提示“无法自动切换”的问题。
